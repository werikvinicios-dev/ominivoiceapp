# Dependências do aplicativo web (leves — não incluem o modelo).
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Para usar o motor real de síntese, instale também o OmniVoice e o PyTorch.
# Veja o README (seção "Backend real"):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install omnivoice
